#!/usr/bin/env node

// Hashbang Grammar -> Hashbangs #! are a sequence of characters at the beginning of a unix-based script which determine what interpreter the script will be executed with


console.log('Hello, JS!'); // This JavaScript file can now be executed directly from a Unix-like command line, outputting “Hello, JS!” to the console.

